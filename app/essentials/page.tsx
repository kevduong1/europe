import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Essentials",
  description: "Eurail notes, packing for the huts, flights, and documents.",
};

export default function EssentialsPage() {
  return null;
}
