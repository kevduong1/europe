# Europe 2026

Mobile-first trip companion for *Munich · Innsbruck · Dolomites · Venice, Sept 5–14, 2026*.

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Itinerary lives in `data/trip.ts`; route geometry in `data/route.ts`.
